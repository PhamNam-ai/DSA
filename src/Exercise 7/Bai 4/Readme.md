Tóm tắt: Viết thêm vào thủ tục partition(int[] ar) để chia mảng thành 2 phần nhỏ hơn và lớn hơn phần tử chốt (được chọn là đầu của mảng). In kết quả sau phân chia.
Đã có sẵn phần code nhập dữ liệu đầu vào, và in mảng ra printArray(int[] ar).
