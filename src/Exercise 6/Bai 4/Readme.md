Tóm tắt: Viết thêm vào thủ tục insertionSortPart2(int[] ar) để sắp xếp theo thuật toán Insertion Sort. Sau mỗi lần (vòng lặp) chèn được một số, in ra trạng thái mảng.

Đã có sẵn phần code nhập dữ liệu đầu vào, và in mảng ra printArray(int[] ar) (ở phía dưới).
Có thể sử dụng insertIntoSorted đã làm ở bài 3 trước vào bài này. Ví dụ: thêm vào hàm đó tham số idx để giúp chèn vị trí tại idx vào danh sách [0 .. idx-1] trước nó.
